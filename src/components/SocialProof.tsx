const SocialProof = () => {
  return (
    <section className='social-proof'>
      <p>
        For independent builders and focused teams who want more output with
        less noise.
      </p>
    </section>
  );
};

export default SocialProof;
