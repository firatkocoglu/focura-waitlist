const SocialProof = () => {
  return (
    <section className='trust-strip'>
      <div>
        <strong>Built for iOS</strong>
        <span>Designed for speed and clarity on mobile</span>
      </div>
      <div>
        <strong>Minimal by default</strong>
        <span>Only the tools needed to execute consistently</span>
      </div>
      <div>
        <strong>Waitlist open</strong>
        <span>Join early and get launch updates first</span>
      </div>
    </section>
  );
};

export default SocialProof;
